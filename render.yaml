services:
  - type: web
    name: WormGPT
    env: python
    plan: free
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: python WormGPT.py
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
